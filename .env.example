# Presearch API Configuration
# Replace with your actual OAuth access token from Presearch
# Get your token from: https://presearch-search-api.readme.io/reference/authentication
PRESEARCH_API_KEY=your_oauth_access_token_here

# Correct base URL based on Presearch API documentation
PRESEARCH_BASE_URL=https://na-us-1.presearch.com

# Optional: Enable debug logging
# DEBUG=presearch-mcp
# VERBOSE=true

# Optional: Configure rate limiting (requests per minute)
# RATE_LIMIT=100

# Optional: Configure request timeout (milliseconds)
# REQUEST_TIMEOUT=15000

# Optional: Configure retry settings
# MAX_RETRIES=3
# RETRY_BASE_DELAY=1000
# RETRY_MAX_DELAY=10000
# RETRY_BACKOFF_FACTOR=2